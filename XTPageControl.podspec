Pod::Spec.new do |s|
  s.name         = "XTPageControl"
  s.version      = "0.0.3"
  s.summary      = "An easy solution to page controllers."
  s.homepage     = "https://github.com/imchenglibin/XTPageControl"
  s.license      = "MIT"
  s.authors       = { "小天" => "2248247504@qq.com" }
  s.platform     = :ios, "7.0"
  s.source       = { :git => "https://github.com/imchenglibin/XTPageControl.git", :tag => s.version }
  s.source_files  = "XTPageControl/**/*.{h,m}"
end
